# cts-dash-application

### in order to create docker container:
```bash
docker build -t cts-dash-application .
```
### in order to run docker container:
```bash
docker run -p 8095:8095 cts-dash-application
```